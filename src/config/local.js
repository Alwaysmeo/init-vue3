// server
const SERVER_API = 'http://localhost:2003'

// hrm
const HMR_OVERLAY = true // 热更新
const LOCAL_HOST = 'localhost' // 本地host
const PORT = 2002 // 本地port

export { SERVER_API, LOCAL_HOST, PORT, HMR_OVERLAY }
